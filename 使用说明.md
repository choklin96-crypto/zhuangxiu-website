# 装修掌门人 · 个性半包报价站

静态页可直接丢到 GitHub Pages；需要登录/验证码时再跑本地 `start.sh`。

首页「我的作品」已并入设计师作品集（理念 / 服务 / 奖项 / 代表作），实景案例读 `works/` 下 16 组相册（来自 2026-08 作品.zip，已压到约 41MB）。点开可左右翻图。

线上参考：https://choklin96-crypto.github.io/zhuangxiu-website/

## 打包（静态站）

把下面这些打进压缩包或推到 Pages 仓库根目录即可：

- `index.html`
- `cover.jpg`（封面实景图）
- `wechat-qrcode.jpg`
- `qrcode-poster.jpg`（分享图）
- `pdf-images/`（产品手册预览图）
- `robots.txt`（可选）

封面为实景图 `cover.jpg`（奶油色极简室内：奶油墙 + 壁灯 + 木质玄关桌 + 胡桃木门），上面叠暖纱罩层保证文字可读；替换封面时用同名文件覆盖 `cover.jpg` 即可（建议竖图、宽 1400px 左右）。原沙漠风代码特效层保留在代码里但已停用（`.hero-fx { display:none }`，删掉这行即可回退）。进站有品牌开场动画（金色 logo 描线 + 字距展开，每次会话播一次，可点按跳过，`prefers-reduced-motion` 时自动跳过）；封面为居中品牌锁定版式：logo + 衬线金字「装修掌门人」（字面流光）+ 金色分隔线 + 长字距副标。工期规划、付款节点、避坑清单、验收清单、装修百问五个功能页数据为占位示例，正式数据在 `index.html` 内 `TIMELINE_STAGES` / `PAYMENT_PLAN` / `PITFALLS` / `ACCEPT_STAGES` / `FAQS` 数组中替换。

不需要 `server/`。GitHub Pages 打开后首页即可用；产品手册密码仍是 `0714`。

## 本地带后端

```bash
chmod +x start.sh
./start.sh
```

浏览器打开 http://localhost:3000/

首次会自动 `npm install`。验证码默认打在终端（`SMS_MODE=mock`）。

复制 `server/.env.example` 为 `server/.env` 后可改端口和 `JWT_SECRET`。

前端 API 地址：

- 本页由 `start.sh` 提供时走同源 `/api`
- 静态页用 `python -m http.server` 时自动指向 `http://localhost:3000/api`
- GitHub Pages 等静态托管不请求登录接口
- 也可在页面里设置 `window.ZHUANGXIU_API = 'https://你的域名/api'`

## 本次相对原站的改动

- **UI 全面美化**：移植本仓库（picture）`cursor/beautiful-ui-clone-fb2b` 分支 Beautiful UI 组件库的设计令牌与组件视觉（CSS 变量令牌体系、SpotlightCard 鼠标径向高光、PushButton 3D 按压主按钮、Shimmer 流光、RecordsTable 表格、辉光复选框、fade-up/pop-in 入场动效），色板映射为沙漠主题（暖沙底 `#f7f2e8` / 品牌橙 `#e85d04` / 赤陶 `#b3541e`），全部以原生 CSS/JS 实现，保持零构建、纯静态部署。功能逻辑、五个数据数组与 localStorage 键名（`renovation_calc` / `zx_pitfalls_v1` / `zx_accept_v1`）均未改动。

- **吸纳开源装修站能力**（内容移植，保持纯静态、无 API）：
  - 报价 / 工期 / 付款 / 验收 / 避坑 / 收房 / 需求表 / 漏项：**导出 CSV 表格**（带 BOM，Excel / WPS 可开）与 **浏览器打印 PDF**
  - **收房验收**独立页（交房验房，与开工后装修验收分开），进度键 `zx_handover_v1`
  - **设计需求表**（按房间填写），自动保存 `zx_req_v1`
  - 计算器 **经济 / 舒适 / 品质** 档次芯片（只填主材参考价，不改半包公式）+ SVG **费用构成饼图**
  - **报价漏项核对**（签约前对照报价单），进度键 `zx_gaps_v1`
  - **监工签字单**：7 个工种（拆改/成品保护/水电/瓦工/木工/油工/竣工）共 73 项，带合格标准数值，逐项判定「合格/不合格」+ 双方签字栏，可打印/导出，进度键 `zx_inspect_v1`；水电/泥木/竣工工种标注对应付款节点
  - **老房翻新流程**：二手房/旧房 11 阶段指南（含外门窗更换、燃气暖气改管等老房特有环节），手风琴展开，可导出
  - **装修锦囊**：环保选材 / 收纳规划 / 儿童房三个专题共 21 条要点（内容整理自 zx029w/zhuangxiu-skills，MIT），可导出
  - 首页排版：报价卡与工具入口拆开，工具区双列磁贴，小屏保留双列避免超长滚动

- 封面改为 ANYU 风格动态特效（鼠标移动会变色），删掉 479KB 的 `cover.jpg`
- 压缩手册图（原站约 17MB，现约 3MB）
- 产品手册密码通过后在线看 `pdf-images/`，不再要求先选 571MB 本地 PDF
- 补 SEO / Open Graph（分享图用 `qrcode-poster.jpg`）
- 文案统一为「装修掌门人 · 个性半包」
- 后端默认 mock 短信；验证码过期时间改用 SQLite `datetime`，避免 ISO 字符串对不上
- `start.sh` 会装依赖，并同时提供静态页和 API；`/server` 不对外暴露数据库

说明：原仓库里 `pdf-images/page_1.jpg` … `page_20.jpg` 是同一张占位图。换成真实手册页时保持文件名即可。
